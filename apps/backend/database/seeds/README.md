# Seed Data - Тестовый датасет для Advocata

## 📊 Что включено

Полный датасет для наглядной демонстрации приложения:

### Пользователи
- **50 клиентов** - различные профили с аватарами
- **30 юристов** - от топовых до начинающих

### Юристы
**Распределение по рейтингу:**
- 🌟 5 топовых (рейтинг 4.8-5.0) - 12-15 лет опыта
- 🌟 5 хороших (рейтинг 4.5-4.7) - 6-10 лет опыта
- 🌟 5 средних (рейтинг 4.0-4.4) - 3-5 лет опыта
- 🌟 5 новых (рейтинг 3.5-3.9) - 1-2 года опыта
- 🌟 10 очень новых (без рейтинга или < 3.5) - 0-1 год опыта

**Специализации:**
- ДТП и автоправо
- Уголовное право
- Семейное право (разводы, алименты)
- Трудовое право (увольнения, невыплаты)
- Гражданское право
- Защита прав потребителей
- Административное право (ГИБДД, штрафы)
- Жилищное право
- Налоговое право

### Консультации
- **150 консультаций** - различные статусы и типы
- Типы: chat, video, call, in_person
- Статусы: completed, in_progress, scheduled, cancelled

### Отзывы
- **~200 отзывов** на юристов
- Распределение оценок:
  - 5 звёзд: ~60%
  - 4 звезды: ~25%
  - 3 звезды: ~10%
  - 2 звезды: ~3%
  - 1 звезда: ~2%
- Реалистичные комментарии на русском

### Экстренные вызовы
- **30 emergency calls** с координатами в Санкт-Петербурге
- Различные статусы: pending, accepted, completed, cancelled

### Документы
- **16 шаблонов документов** по категориям
- Бесплатные и Premium документы
- Счётчики скачиваний

### Дополнительно
- Сохранённые адреса пользователей
- Экстренные контакты
- Реферальные коды

---

## 🚀 Как использовать

### Вариант 1: Через psql (рекомендуется)

```bash
# Перейдите в папку seeds
cd /home/user/advocata/apps/backend/database/seeds

# Загрузите данные в БД
psql -U advocata -d advocata_db -f seed-data.sql
```

### Вариант 2: Через npm script

Добавьте в `package.json` (apps/backend):

```json
{
  "scripts": {
    "seed": "psql -U advocata -d advocata_db -f database/seeds/seed-data.sql"
  }
}
```

Затем:
```bash
npm run seed
```

### Вариант 3: Через Docker

```bash
docker exec -i advocata-postgres psql -U advocata -d advocata_db < seed-data.sql
```

---

## ⚠️ Важно!

### Перед загрузкой

**Датасет перезаписывает существующие данные!**

Закомментированная строка в начале файла:
```sql
-- TRUNCATE TABLE consultation_reviews, consultations, lawyer_specializations,
-- lawyer_documents, lawyers, user_addresses, emergency_contacts, users CASCADE;
```

Если нужно **полностью очистить БД** перед загрузкой - раскомментируйте эту строку.

### Что делать если есть конфликты ID

Если при загрузке возникают ошибки duplicate key, выполните сначала:

```sql
-- Очистите только тестовые данные
TRUNCATE TABLE consultation_reviews, consultations,
  lawyer_specializations, lawyers,
  user_addresses, emergency_contacts,
  referral_codes, emergency_calls,
  users CASCADE;
```

---

## 📱 Как посмотреть в приложении

### Mobile App

После загрузки данных:

1. **Главный экран** → "Консультация"
2. **Поиск юристов** → увидите 30 юристов с разными рейтингами
3. **Фильтры** → фильтруйте по специализации (ДТП, Семейное право и т.д.)
4. **Профиль юриста** → откройте любого юриста, увидите:
   - Рейтинг и количество консультаций
   - Специализации
   - Биографию
   - Отзывы клиентов
   - Стоимость консультации

5. **Документы** → "Шаблоны документов"
   - Увидите 16 документов в разных категориях
   - Счётчики скачиваний

### Admin Panel

После загрузки данных:

1. **Dashboard** → увидите статистику:
   - 80 пользователей
   - 30 активных юристов
   - 150+ консультаций
   - Выручка

2. **Pending Lawyers** → `/lawyers/pending`
   - Здесь будут новые юристы (можно добавить вручную со статусом `pending`)

3. **Active Lawyers** → `/lawyers`
   - 30 активных юристов
   - Фильтры по специализации, рейтингу
   - Сортировка

---

## 🔍 Примеры запросов

### Топ-5 юристов по рейтингу

```sql
SELECT
  u.first_name || ' ' || u.last_name as name,
  l.rating,
  l.total_consultations,
  l.hourly_rate
FROM lawyers l
JOIN users u ON l.user_id = u.id
WHERE l.rating IS NOT NULL
ORDER BY l.rating DESC, l.total_consultations DESC
LIMIT 5;
```

### Юристы по специализации "ДТП"

```sql
SELECT
  u.first_name || ' ' || u.last_name as name,
  l.rating,
  l.experience_years,
  ls.specialization
FROM lawyers l
JOIN users u ON l.user_id = u.id
JOIN lawyer_specializations ls ON l.id = ls.lawyer_id
WHERE ls.specialization ILIKE '%ДТП%'
ORDER BY l.rating DESC NULLS LAST;
```

### Статистика отзывов

```sql
SELECT
  rating,
  COUNT(*) as count,
  ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER (), 2) as percentage
FROM consultation_reviews
GROUP BY rating
ORDER BY rating DESC;
```

### Последние 10 экстренных вызовов

```sql
SELECT
  u.first_name || ' ' || u.last_name as client,
  ec.address,
  ec.description,
  ec.status,
  ec.created_at
FROM emergency_calls ec
JOIN users u ON ec.user_id = u.id
ORDER BY ec.created_at DESC
LIMIT 10;
```

---

## 🎨 Кастомизация

### Добавить своих юристов

Отредактируйте секцию "2. ЮРИСТЫ/АДВОКАТЫ" в `seed-data.sql`:

```sql
INSERT INTO users (...) VALUES
('ваш-uuid', 'email@example.com', '+79219001001', 'Имя', 'Фамилия', ...);

INSERT INTO lawyers (...) VALUES
(gen_random_uuid(), 'ваш-uuid', 'номер_лицензии', 'образование', ...);
```

### Изменить количество данных

Измените параметры в `generate_series`:

```sql
-- Было: 150 консультаций
FROM generate_series(1, 150) AS s

-- Стало: 300 консультаций
FROM generate_series(1, 300) AS s
```

---

## 📊 Структура датасета

```
users (80 total)
├── clients (50)
└── lawyers (30)
    ├── lawyer_profiles (30)
    ├── lawyer_specializations (~60)
    └── consultations (150)
        └── consultation_reviews (~200)

emergency_calls (30)
documents (16)
user_addresses (~20)
emergency_contacts (~20)
referral_codes (20)
```

---

## 🐛 Troubleshooting

### Ошибка: "duplicate key value"

**Решение:** Очистите таблицы перед загрузкой:
```sql
TRUNCATE TABLE users CASCADE;
```

### Ошибка: "constraint violation"

**Решение:** Проверьте, что все миграции применены:
```bash
npm run migration:run
```

### Данные не отображаются в приложении

**Решение:** Проверьте, что backend подключен к правильной БД:
```bash
# Проверьте .env файл
cat apps/backend/.env | grep DATABASE
```

---

## ✅ Проверка успешной загрузки

```sql
-- Проверьте количество записей
SELECT
  'users' as table_name, COUNT(*) as count FROM users
UNION ALL
SELECT 'lawyers', COUNT(*) FROM lawyers
UNION ALL
SELECT 'consultations', COUNT(*) FROM consultations
UNION ALL
SELECT 'reviews', COUNT(*) FROM consultation_reviews
UNION ALL
SELECT 'emergency_calls', COUNT(*) FROM emergency_calls
UNION ALL
SELECT 'documents', COUNT(*) FROM documents;
```

**Ожидаемый результат:**
```
table_name      | count
----------------+-------
users           | 80
lawyers         | 30
consultations   | 150
reviews         | ~200
emergency_calls | 30
documents       | 16
```

---

## 📞 Тестовые учётные записи

### Клиенты (для входа в Mobile App)

```
Email: ivan.petrov@mail.ru
Phone: +79219876543
Имя: Иван Петров

Email: maria.smirnova@yandex.ru
Phone: +79219876544
Имя: Мария Смирнова
```

### Юристы (для входа как юрист)

```
Email: alexey.petrov.law@advocata.ru
Phone: +79219001001
Имя: Алексей Петров (ТОП юрист, рейтинг 4.95)

Email: maria.smirnova.law@advocata.ru
Phone: +79219001002
Имя: Мария Смирнова (ТОП юрист, рейтинг 5.0)
```

---

## 🎯 Что дальше?

После загрузки датасета:

1. ✅ Запустите мобильное приложение
2. ✅ Откройте раздел "Консультация"
3. ✅ Посмотрите список юристов
4. ✅ Откройте профиль юриста
5. ✅ Прочитайте отзывы
6. ✅ Проверьте документы
7. ✅ Откройте Admin Panel
8. ✅ Посмотрите дашборд со статистикой

**Приложение готово к демонстрации! 🎉**

---

**Создано:** November 18, 2025
**Версия датасета:** 1.0
**Совместимость:** PostgreSQL 14+, PostGIS
